<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="img/logomark.png" />
    <title>Login</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/style.css" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap"
      rel="stylesheet"
    />
    <!-- Fonts -->
  </head>
  <body>
    <div class="container">
      <div class="row mt-5 pt-5 align-items-center justify-content-center">
        <div class="col-md-5">
          <div class="card reg-form-c p-4">
            <div class="card-body">
              <h2 class="card-title">Login</h2>
              <%- include("partial/messages.ejs") %>
              <form action="/users/login" method="POST">
                <div class="row">
                  <label for="email" class="form-label fw-semibold" name="email"
                    >Email</label
                  >
                  <input
                    id="email"
                    type="email"
                    class="form-control w-100"
                    name="email"
                  />
                </div>
                <div class="row mt-3 mb-5">
                  <label
                    for="pass"
                    class="form-label fw-semibold"
                    name="password"
                    >Password</label
                  >
                  <input
                    id="pass"
                    type="password"
                    class="form-control w-100"
                    name="password"
                  />
                </div>
                <div class="row mt-3">
                  <button
                    type="submit"
                    class="btn btn-sm btn-outline-light p-2"
                  >
                    Sign in
                  </button>
                </div>
                <div class="row mt-5">
                  <a href="/users/new-reg" class="text-light col pe-0"
                    >New Registration</a
                  >
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
